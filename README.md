# sneaky-link

To install dependencies:

```bash
bun install
```

To run tests:

```bash
bun run test
```

To run cli with visible browser (helpful during development):

```bash
bun run cli:visible
```
